const myNumber: number = 20; 
let myString: string = "надо поспать";
const myBoolean: boolean = true;

let myUndefined: undefined = undefined;
let myNull: null = null;
const mySymbol: symbol = Symbol();

let myArray: number[] = [1, 2, 3, 4, 5];
let myTuple: [string, number] = ["Возраст", 20];

console.log(myNumber, myString, myBoolean, myUndefined, myNull, mySymbol, myArray, myTuple);
